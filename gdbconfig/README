init and alias files are .gitconfig files, to be placed in ~/.gitconfig.d/ and used in conjunction with gdbdashboard:
https://github.com/cyrus-and/gdb-dashboard

you need to install pygments for the syntax-highlighting to work:

	pip3 install pygments
